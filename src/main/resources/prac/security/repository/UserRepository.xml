<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="prac.security.repository.UserRepository">

    <select id="findByEmail" parameterType="String" resultType="prac.security.model.User" >
        SELECT
            user_id,
            firstname,
            lastname,
            password,
            email,
            enabled
        FROM
            user_info
        WHERE
            email = #{email}
    </select>


    <insert id="save" parameterType="prac.security.model.User" useGeneratedKeys="true" keyProperty="user_id">
    	INSERT INTO user_info (firstname, lastname, password, email, enabled)
    	VALUES
    	(#{firstname}, #{lastname}, #{password}, #{email}, #{enabled} )

    	<selectKey resultType="long" keyProperty="user_id" order="AFTER">
			select @@IDENTITY
		</selectKey>
    </insert>

</mapper>